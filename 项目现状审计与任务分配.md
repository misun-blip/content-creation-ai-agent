# 项目现状审计 & 新团队任务分配

---

**编制日期**：2026年3月27日  
**编制人**：队长  
**目的**：对比文档规划与代码实现，明确未完成功能，为新团队分配具体开发任务

---

## 1. 功能完成度总览

| 模块 | 计划功能 | 完成状态 | 说明 |
|------|---------|:--------:|------|
| **用户认证** | 注册/登录/JWT | ✅ 已完成 | `auth.py` + `auth_service.py` |
| **用户认证** | 修改密码 | ✅ 已完成 | `auth.py` 中 `/change-password` |
| **用户认证** | 个人信息管理/头像上传 | ❌ **未实现** | `UserProfile` 模型不存在 |
| **素材管理** | 素材 CRUD | ✅ 已完成 | `materials.py` |
| **素材管理** | 标签管理 | ✅ 已完成 | `tags.py` + `tag.py` 模型 |
| **素材管理** | 文件上传 | ✅ 已完成 | `uploads.py` |
| **素材管理** | 分类管理 (categories) | ❌ **未实现** | 文档设计了 `categories` 表，但无模型、无 API |
| **素材管理** | RAG 语义检索 (ChromaDB) | ❌ **未实现** | 无 ChromaDB 集成，仅有关键词模糊搜索 |
| **AI 生成** | 选题推荐 | ✅ 已完成 | `ai.py` 中 `generate_topics` |
| **AI 生成** | 文案生成 | ✅ 已完成 | `ai.py` 中 `generate_content` |
| **AI 生成** | **SSE 流式输出** | ❌ **未实现** | 当前是一次性返回全部内容，非流式 |
| **AI 生成** | AI 配图生成 | ❌ **未实现** | 无图像生成 API 调用 |
| **AI 生成** | 质量评估 | ✅ 已完成 | `evaluate_content` 规则评分 |
| **平台适配** | 抖音/小红书/微信 | ✅ 已完成 | `adapter_service.py` + 策略模式 |
| **平台适配** | 合规审计（违禁词） | ✅ 已完成 | `adapter_service.py` 内置词库 |
| **创作记录** | 记录 CRUD | ✅ 已完成 | `records.py` 完整实现 |
| **创作记录** | 版本管理/对比/回滚 | ✅ 已完成 | 版本创建、列表、回滚、删除均已实现 |
| **创作记录** | 导出 (CSV/TXT/Word/PDF) | ✅ 已完成 | `records.py` 四种格式导出 |
| **统计分析** | 仪表盘数据 | ✅ 已完成 | `dashboard.py` 总览 API |
| **统计分析** | **热点话题感知** | ❌ **未实现** | 无热搜/热榜 API 对接 |
| **基础设施** | Docker 部署 | ✅ 已完成 | `docker-compose.yml` + Dockerfile |
| **基础设施** | Redis 缓存 | ❌ **未实现** | Redis 容器已配置但代码中无实际缓存调用 |
| **基础设施** | CI/CD | ❌ **未实现** | 无 `.github/workflows` |

---

## 2. 未实现功能优先级

### P0 核心（必须实现）

| # | 功能 | 工作量 |
|---|------|--------|
| 1 | SSE 流式文案生成 | 中 |
| 2 | config.py 补充 AI 配置字段 | 小 |
| 3 | Alembic 迁移重建 | 小 |

### P1 重要（应该实现）

| # | 功能 | 工作量 |
|---|------|--------|
| 4 | 分类管理 (categories) | 中 |
| 5 | RAG 语义检索 (ChromaDB) | 大 |
| 6 | 热点话题感知 | 中 |
| 7 | Redis 缓存落地 | 中 |

### P2 增强（锦上添花）

| # | 功能 | 工作量 |
|---|------|--------|
| 8 | AI 配图生成 | 中 |
| 9 | 用户个人信息/头像 | 小 |
| 10 | Quill 富文本编辑器 | 小 |
| 11 | CI/CD (GitHub Actions) | 小 |

---

## 3. 新团队任务分配（4人 x 3周）

### 3.1 队长 - 架构修复 + AI 核心

- [ ] 修复 config.py，补 AI_API_KEY / AI_API_BASE 字段
- [ ] 重建 Alembic 迁移
- [ ] 实现 SSE 流式文案生成
- [ ] 集成 ChromaDB RAG 语义检索
- [ ] AI 配图生成接口
- [ ] 代码审查全部 PR

### 3.2 前端开发 - 页面完善

- [ ] ContentGenerate.vue 对接 SSE 流式渲染
- [ ] 集成 Quill 富文本编辑器
- [ ] Profile.vue 个人信息与头像页面
- [ ] 分类管理前端组件
- [ ] 热点话题展示卡片
- [ ] 响应式布局优化

### 3.3 后端开发 - 数据模型 + 业务 API

- [ ] 新建 Category 模型 + CRUD API
- [ ] UserProfile 模型 + 个人信息 API
- [ ] 热点话题 API（对接热搜）
- [ ] Redis 缓存落地
- [ ] 数据库查询优化

### 3.4 测试运维 - 质量保障

- [ ] 搭建 GitHub Actions CI/CD
- [ ] API 接口测试（Postman 集合）
- [ ] 系统集成测试 + 性能测试
- [ ] 部署文档、用户手册编写

---

## 4. Git 协作规范

### 4.1 分支说明

本项目采用 **「主分支 + 个人分支」** 模式，以 **成员名字** 命名分支。

| 分支 | 命名规则 | 用途 | 权限 |
|------|---------|------|------|
| **main** | `main` | 稳定发布分支，始终可运行 | 所有人不能直接提交，仅队长通过 PR 合并 |
| **develop** | `develop` | 开发主分支，所有人代码汇入此处 | 不能直接提交，只能通过 PR 合入 |
| **个人分支** | `dev/成员名` | 每个人的专属开发分支 | 本人自由提交 |
| **修复分支** | `bugfix/问题描述` | 紧急 Bug 修复（临时创建，修完即删） | 谁发现谁建 |

分支关系图：

```
main（稳定版，每周五合并一次）
  |
  +-- develop（开发版，所有个人分支往这里提 PR）
       |
       +-- dev/zhangsan     <-- 队长的个人分支
       +-- dev/lisi         <-- 前端开发的个人分支
       +-- dev/wangwu       <-- 后端开发的个人分支
       +-- dev/zhaoliu      <-- 测试运维的个人分支
```

核心规则（必须遵守）：

- 禁止任何人直接往 main 或 develop 提交代码
- 所有代码改动都在自己的个人分支上开发
- 开发完成后提交 PR（Pull Request）请求合并到 develop
- 每个 PR 至少 1 人审查（队长必审），审查通过后才能合并

---

### 4.2 新建分支详细操作

#### A. 队长创建 develop 分支（整个项目只做一次）

```bash
# 进入项目目录
cd content-creation-ai-agent

# 确保在 main 分支上
git checkout main

# 基于 main 创建 develop 分支
git checkout -b develop
# 解释：checkout = 切换分支，-b = 同时新建，所以这条命令 = 新建 develop 并切换过去

# 推送到 GitHub
git push -u origin develop
# 解释：-u = 设置默认远端，以后直接 git push 就行

# 验证：现在 GitHub 上应该能看到 main 和 develop 两个分支了
```

#### B. 每个成员创建自己的个人分支（每人做一次）

```bash
# 第 1 步：克隆仓库（如果还没有 clone）
git clone https://github.com/misun-blip/content-creation-ai-agent.git
cd content-creation-ai-agent

# 第 2 步：切到 develop 分支，拉取最新代码
git checkout develop
git pull origin develop

# 第 3 步：基于 develop 创建你的个人分支
# -----------------------------------------------
# 用你名字的拼音，全小写，例如：
#
#   队长张三执行：   git checkout -b dev/zhangsan
#   前端李四执行：   git checkout -b dev/lisi
#   后端王五执行：   git checkout -b dev/wangwu
#   测试赵六执行：   git checkout -b dev/zhaoliu
# -----------------------------------------------
git checkout -b dev/你的名字

# 第 4 步：推送到 GitHub
git push -u origin dev/你的名字
# 例如：git push -u origin dev/zhangsan

# 完成！以后所有代码都在这个分支上写
```

**怎么确认自己在哪个分支？**

```bash
git branch
# 会列出所有本地分支，当前分支前面有 * 号
# 例如：
#   develop
# * dev/zhangsan    <-- 带星号的就是当前分支
#   main
```

---

### 4.3 日常开发流程

#### 第一步：开始写代码前先同步

每天开始写代码之前，先执行以下命令拿到别人的最新代码：

```bash
# 切到 develop 拉最新
git checkout develop
git pull origin develop

# 切回自己的分支
git checkout dev/你的名字

# 把 develop 的更新合并到自己的分支
git merge develop
# 如果提示冲突 -> 看 4.7 节
```

#### 第二步：写代码并提交

```bash
# 正常写代码……改完之后：

git status                    # 看改了哪些文件
git add .                     # 把所有改动添加到暂存区（. = 全部文件）
git commit -m "feat: 实现SSE流式文案生成后端接口"    # 提交
git push                      # 推送到 GitHub
```

#### 第三步：提交 PR 请求合并

功能写完后，去 GitHub 网页操作：

```
1. 打开 https://github.com/misun-blip/content-creation-ai-agent

2. 页面顶部会出现黄色横条提示：
   "dev/zhangsan had recent pushes - Compare & pull request"
   点击【Compare & pull request】

3. 确认合并方向：
   base: develop  <--  compare: dev/zhangsan
   （意思是：把 dev/zhangsan 的改动合并到 develop）

4. 填写标题，例如：[AI] 实现SSE流式文案生成

5. 填写描述（按 4.5 节的模板写）

6. 右侧 Reviewers 选择【队长】

7. 点击绿色按钮【Create pull request】

8. 等待队长审查
```

什么是 PR？简单说就是你跟队长说："我代码写好了请帮我看一下，没问题就合并到 develop 分支。" 队长会在 GitHub 上逐行看你的代码改动。

#### 第四步：队长审查并合并

```
1. 队长在 GitHub 收到通知 -> 点进 PR 查看代码改动

2. 如果有问题 -> 在代码行上点 + 号留评论，说明要修改什么

3. 开发者改完后 git push，PR 自动更新

4. 没问题 -> 队长点【Merge pull request】->【Squash and merge】

5. 合并完成后点【Delete branch】清理远端分支
```

#### 第五步：合并后所有人同步

每次有 PR 被合并后，所有成员都要同步：

```bash
git checkout develop
git pull origin develop

git checkout dev/你的名字
git merge develop
```

---

### 4.4 Commit 规范

格式：**类型: 描述**（冒号后有一个空格，描述用中文）

| 类型 | 什么时候用 | 例子 |
|------|-----------|------|
| feat | 新增功能 | feat: 实现SSE流式文案生成 |
| fix | 修复 Bug | fix: 修复登录Token过期白屏 |
| docs | 改文档 | docs: 更新API文档 |
| refactor | 重构（不改功能） | refactor: 抽取平台适配公共方法 |
| test | 加测试 | test: 添加素材管理单元测试 |
| chore | 改配置 | chore: 更新Docker配置 |

---

### 4.5 PR 描述模板

```
## 改动说明
简要描述做了什么（1-2句话）

## 改动类型
- [ ] 新功能 (feat)
- [ ] Bug 修复 (fix)
- [ ] 重构 (refactor)
- [ ] 文档 (docs)

## 改了哪些文件
- backend/app/api/v1/ai.py - 新增 SSE 流式接口
- frontend/src/views/ai/ContentGenerate.vue - 对接 EventSource

## 怎么测试
1. 启动后端 python run.py
2. 启动前端 npm run dev
3. 进入 AI 文案生成页面，输入关键词，看是否逐字输出

## 截图（有界面改动的话贴截图）
```

---

### 4.6 常用 Git 命令速查表

| 场景 | 命令 |
|------|------|
| 查看当前在哪个分支 | `git branch` |
| 切换到某个分支 | `git checkout 分支名` |
| 创建新分支并切换 | `git checkout -b 新分支名` |
| 查看文件改动状态 | `git status` |
| 添加所有改动到暂存区 | `git add .` |
| 提交 | `git commit -m "描述"` |
| 推送到 GitHub | `git push` |
| 拉取最新代码 | `git pull origin 分支名` |
| 合并某个分支到当前分支 | `git merge 分支名` |
| 查看提交历史 | `git log --oneline -10` |

---

### 4.7 冲突解决

当 git merge 或 PR 提示冲突时：

```bash
# Git 会在文件中插入冲突标记：
<<<<<<< HEAD
你写的代码
=======
别人写的代码
>>>>>>> develop

# 解决方法：
# 1. 用编辑器打开冲突文件
# 2. 决定保留哪部分代码（或两部分都保留）
# 3. 删掉 <<<<<<< ======= >>>>>>> 标记符号
# 4. 保存文件

git add .
git commit -m "fix: 解决与develop的合并冲突"
git push
```

预防冲突的好习惯：
- 每天开始写代码前先同步 develop
- 尽量不要和别人改同一个文件
- 频繁提交 PR，不要攒很多改动

---

### 4.8 每周发布（队长操作）

每周五将 develop 合并到 main 并打版本标签：

```bash
git checkout main
git pull origin main
git merge develop
git tag -a v1.0 -m "第一周：SSE流式生成 + 分类管理"
git push origin main --tags
```

---

## 5. 开发环境搭建（新成员必读）

### 5.1 前置条件

- Node.js 18+
- Python 3.10+
- Docker Desktop
- Git

### 5.2 快速启动

```bash
# 1. 克隆仓库
git clone https://github.com/misun-blip/content-creation-ai-agent.git
cd content-creation-ai-agent

# 2. 启动数据库（确保本机 MySQL 服务已停止）
net stop mysql80    # Windows 管理员权限
docker-compose up -d mysql redis

# 3. 后端（新终端）
cd backend
python -m venv venv
.\venv\Scripts\activate
pip install -r requirements.txt
cp .env.example .env       # 然后编辑 .env 填入自己的 AI_API_KEY
python run.py              # http://localhost:8000

# 4. 前端（再开一个新终端）
cd frontend
npm install
npm run dev                # http://localhost:3000

# 5. 默认账号：admin / admin123
```

---

本文档基于对项目全部源代码与设计文档的逐项对比审计所得，为新团队提供明确的任务清单和协作规范。
